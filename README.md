# Agent Knowledge Base

A semantic knowledge base with streaming retrieval and MCP integration for agents.

## Overview

This project will implement a lightweight knowledge base service that supports text ingestion, semantic search, streaming responses, and an MCP tool interface for agent workflows.

## Current Status

Project skeleton initialized. Core implementation will be added step by step.
